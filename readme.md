# Hello

```bash
git config user.name "laladaysz"
git config user.email "leandralaura.dias@gmail.com"
```

```bash
git config user.name "laladaysz"
git config user.email "leandralaura.dias@gmail.com"

git clone <url do git>

git add .

git commit -m "sua msg"

git push
```